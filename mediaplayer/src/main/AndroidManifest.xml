<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.com.igorka.oa.android.mediaplayer" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VideoViewActivity"
            android:label="@string/title_activity_video_view" >
        </activity>
        <activity
            android:name=".MediaRecorderActivity"
            android:label="@string/title_activity_media_recorder" >
        </activity>
        <activity
            android:name=".TakePhotoGalleryActivity"
            android:label="@string/title_activity_take_photo_gallery" >
        </activity>
        <activity
            android:name=".TakePhotoFromCameraActivity"
            android:label="@string/title_activity_take_photo_from_camera" >
        </activity>
        <activity
            android:name=".CastActivity"
            android:label="@string/title_activity_cast" >
        </activity>
    </application>

</manifest>
